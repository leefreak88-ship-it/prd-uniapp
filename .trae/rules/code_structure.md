# 代码结构 (Code Structure)

## 具体约束条件
代码必须严格按照以下顺序组织：
1.  **Imports**：引入模块、组件、工具函数等。
2.  **Interfaces**：类型定义、常量声明。
3.  **Props/State**：组件属性定义、响应式状态声明。
4.  **Methods**：业务逻辑处理函数、计算属性等。
5.  **Lifecycle**：生命周期钩子函数（如 `onMounted`, `onShow` 等）。
